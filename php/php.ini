; ==============================
; Fichier PHP.INI personnalisé
; ==============================

; Limites de ressources
memory_limit = 512M
upload_max_filesize = 64M
post_max_size = 64M
max_execution_time = 300
max_input_time = 300

; Affichage des erreurs (utile en dev)
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
log_errors = On

; Timezone
date.timezone = Europe/Paris

; Sessions
session.gc_maxlifetime = 1440
session.save_path = "/tmp"

; Paramètres utiles WordPress
max_input_vars = 5000

; Extensions (commentées pour éviter erreurs si non présentes — décommente au besoin)
; extension=mysqli
; extension=pdo_mysql
; extension=curl
; extension=gd
; extension=zip
; extension=mbstring
; extension=xml
; extension=soap
; extension=exif
; extension=ftp
; extension=calendar
; extension=openssl
; extension=imagick
; extension=apcu
; extension=redis
; extension=memcached
; extension=soap
; extension=xmlrpc
; extension=zip
; extension=gd